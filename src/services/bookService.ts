const getAllBooks = () => {
  return 'Get all books';
};

const getAllAvailableBooks = () => {
  return 'Get all available books';
};

const getBook = () => {
  return 'Get a book';
};

const addBook = () => {
  return 'Add a book';
};

const updateBook = () => {
  return 'Update a book';
};

const deleteBook = () => {
  return 'Delete a book';
};

export default {
  getAllBooks,
  getAllAvailableBooks,
  getBook,
  addBook,
  updateBook,
  deleteBook
};
